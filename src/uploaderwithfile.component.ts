import { Component } from '@angular/core';

@Component({
    selector: "uploader-with-file",
    template: `<ng-content></ng-content>`
})

/**
 * Modal body
 */
export class UploaderWithFileComponent {
}